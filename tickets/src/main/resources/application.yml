spring:
  datasource: 
    url: jdbc:mysql://localhost:3306/pizza?useSSL=false&allowPublicKeyRetrieval=true&useUnicode=true&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=UTC
    username: pizza
    password: pizza
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    show-sql: false
    hibernate:
      ddl-auto: update    
  thymeleaf:
    cache: false 
server:
  error:
    whitelabel:
      enabled: true
